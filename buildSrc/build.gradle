repositories {
    jcenter()
}

apply from: '../compile-dependencies.gradle'

sourceSets {
    main {
        groovy {
            srcDir '../src/main/groovy'
        }
        resources {
            srcDir '../src/main/resources'
        }
    }
}