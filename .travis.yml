sudo: false
addons:
  apt:
    packages:
    - git
language: java
jdk: oraclejdk7
env: TERM=dumb
cache:
  directories:
  - $HOME/.gradle
before_install: git checkout -qf $TRAVIS_BRANCH
install: ./gradlew release cobertura --info --stacktrace
after_success: ./gradlew coveralls
